<%if object.errors.any?%>
	<h3><%= pluralize(object.errors.count, "error")%> prevents new list from being </h3>
	<ul>
		<%= object.errors.full_messages.each do |message|%>
			<li><%= message%></li>
		<%end%>
	</ul>
<%end%>